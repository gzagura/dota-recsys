# Полный набор: сбор данных, обучение, тесты и сайт.
# Для сервера есть урезанный requirements-serve.txt — там только инференс.
#
# Границы, а не точные версии: проверено на Python 3.12 (pandas 3.0, numpy 2.5)
# и на 3.14 (сервер). Прежние пины вида numpy==1.* под 3.14 не собираются.
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
pandas>=2.2,<4.0
numpy>=1.26,<3.0
pyarrow>=16
pyyaml>=6,<7
lightgbm>=4.5,<5

# ETL из OpenDota
requests>=2.31,<3.0
tqdm>=4.66,<5.0

# Метрики обучения L3 (в рантайме сайта не нужны)
scikit-learn>=1.4,<2.0

pytest>=8.0,<10.0
# Нужен fastapi.testclient — им проверяются сами ручки, а не только модели
httpx>=0.27,<1.0
